/ frozen_string_literal: true
= simple_form_for :ingredient do |f|
  .control-group
    h3 Ingredient
    .form-group
      = f.input :title, autofocus: true

  .form-actions
    = f.submit, class: "btn btn-primary"
    = link_to "Cancel", recipes_path, class: "btn"
